version: "3.9"
services:
  app:
    build: .
    ports:
        - "8080:80"
  db:
    image: "mcr.microsoft.com/mssql/server"
    environment: 
      SA_PASSWORD: "Your_password123" 
      ACCEPT_EULA: "Y"